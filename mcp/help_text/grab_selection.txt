Read the latest grab/selection JSON produced by the app (for example `ai-select`).
Returns the enabled flag, selected entity summary, hierarchy, cursor, and UI/world target details.
Optional parameters let you override the file path, require `enabled=true`, or allow a missing file.
